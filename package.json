{
  "name": "advisor-agency-cms",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/dev.mjs",
    "db:generate": "drizzle-kit generate",
    "test": "node --test tests/cms.test.mjs",
    "test:vercel": "npm run build && node --test tests/vercel.test.mjs"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "@vercel/blob": "^2.8.0"
  },
  "engines": {
    "node": "24.x"
  }
}
