# تشغيل ADVISOR على Vercel

هذه النسخة مبنية من مستودع fatma650/website-advisor، وتحافظ على التصميم ومحتواه وتعديل موضع التمرير. أضيفت مسارات Vercel Functions للمحتوى والأدمن، ومحوّل قاعدة بيانات libSQL، وتخزين Vercel Blob للصور. لا تستخدم بيانات تسجيل الدخول الخاصة بـSites على Vercel.

## 1. ارفعي الملفات

استبدلي ملفات المستودع بمحتويات ZIP من الداخل، دون وضعها في فولدر إضافي. يجب أن يكون package.json وvercel.json وapi/ في جذر المشروع نفسه. ارفعي الملفات الجديدة والمعدلة، وخاصة package-lock.json وserver/ وapi/ وvercel.json. لا ترفعي node_modules أو .env أو dist؛ Vercel يبني dist.

في Vercel → Project Settings → Build and Deployment:

- Root Directory: جذر المستودع الذي فيه package.json، بدون فولدر إضافي.
- Framework Preset: Other.
- Build Command: npm run build.
- Output Directory: dist/client.
- Node.js: 24.x.
- أعيدي النشر بعد التغييرات. vercel.json يحتوي الإعدادات والمسارات المطلوبة؛ أزيلي أي override قديم متعارض في لوحة المشروع.

قبل ربط قاعدة البيانات، الواجهة تعرض المحتوى المرفق من API جاهز للقراءة، دون رسالة تعذر تحميل التحديثات. لوحة الأدمن تعرض رسالة إعداد واضحة وترفض التعديل؛ لا توجد كلمة مرور افتراضية أو تسجيل مفتوح لأول زائر. بعد تهيئة القاعدة، API يقرأ المحتوى الدائم ولا يخفي أخطاء قاعدة البيانات بمحتوى قديم.

## 2. قاعدة بيانات دائمة

أنشئي قاعدة **libSQL** فارغة مخصصة للموقع عبر Turso؛ @libsql/client يحتاج libSQL، وليس اختيار محرك غير متوافق. خذي URL وAuth Token. يمكن ربط Turso من تكامل Vercel إذا كان متاحًا لحسابك، أو إضافة القيم يدويًا.

[مرجع Turso](https://docs.turso.tech/sdk/ts/reference)

في Vercel → Settings → Environment Variables أضيفي القيم التالية لبيئة Production. لا تضعيها في JavaScript العام أو GitHub ولا ترسليها في الشات:

| الاسم | القيمة |
|---|---|
| TURSO_DATABASE_URL | رابط قاعدة libSQL، مثل libsql://… |
| TURSO_AUTH_TOKEN | توكن قاعدة البيانات |
| ADMIN_BOOTSTRAP_EMAIL | بريدك؛ وجهة استعادة الحساب عند إعداد البريد، وليس شرط تسجيل دخول خارجي |
| ADMIN_INITIAL_PASSWORD | باسورد جديد تختارينه، من 12 إلى 128 حرفًا؛ يستخدم مرة واحدة فقط لتهيئة الحساب |
| ADMIN_PASSWORD_PEPPER | قيمة عشوائية سرية ثابتة من 32 حرفًا على الأقل؛ لا تغيريها بعد إنشاء الحساب |

لتوليد pepper محليًا دون تخزينه في ملف عام:

```sh
node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
```

بعد أول دخول ناجح، احذفي ADMIN_INITIAL_PASSWORD من إعدادات Vercel وأعيدي النشر؛ البصمة محفوظة في القاعدة. تغيير هذه القيمة لاحقًا لا يعيد ضبط باسورد موجود. كلمة مرور التجربة المحلية القديمة لا تعمل كباسورد إنتاج.

الجداول تُنشأ مرة واحدة عند أول اتصال، داخل transaction لمنع تداخل تشغيل السيرفرات. استخدمي قاعدة جديدة فارغة؛ لا تربطي قاعدة إنتاج غير مرتبطة بالموقع أو تنسخي إليها جداول D1 يدويًا دون خطة نقل.

محتوى server/seed.json يهيئ القاعدة الجديدة فقط. التعديلات والصور الموجودة في قاعدة استضافة أخرى لا تُنقل تلقائيًا؛ تحتاج تصديرًا من المصدر إن كانت أحدث من ملفات المستودع.

للـPreview استخدمي قاعدة وتخزينًا وأسرارًا منفصلة عن Production. لا تجعلي تجربة فرع تعدّل محتوى الموقع الفعلي.

## 3. تخزين الصور

من Vercel → Storage أنشئي **Blob Store من نوع Public** واربطيه بالمشروع. هذه صور أعمال عامة، وليست ملفات خاصة. تأكدي من وجود BLOB_READ_WRITE_TOKEN في Environment Variables.

[توثيق Vercel Blob](https://vercel.com/docs/vercel-blob/using-blob-sdk)

الصور الأصلية في public/assets تعمل دون Blob. رفع صور جديدة من الأدمن يحتاج هذا الربط. روابط الموقع تبقى /media/…، بينما يتم حفظ الصور ومراجعها بشكل دائم. الحد الجديد لكل صورة على Vercel هو **3 MB** لتناسب حد جسم طلب Functions؛ يمكن ضغطها إلى WebP/JPG. لم ننقل رفع الصور إلى المتصفح بتوكن عام.

## 4. تشغيل الأدمن

أعيدي Deploy بعد ضبط القيم. افتحي /admin وأدخلي ADMIN_INITIAL_PASSWORD الذي اخترتيه. يمكن تغيير الباسورد من لوحة الإدارة. لا تحتاجين تسجيل الدخول بحساب ChatGPT على Vercel.

- الجلسة cookie محمية بـHttpOnly وSecure وSameSite وتعيش 8 ساعات.
- الباسورد يُفحص في السيرفر ببصمة PBKDF2-SHA256 بـ600,000 تكرار وpepper سري.
- كل تعديل ورفع وحذف واسترجاع يحتاج جلسة صالحة وفحص مصدر الطلب.
- محاولات الدخول محدودة بـ5 خلال 15 دقيقة في قاعدة البيانات، وليست عدادًا محليًا في السيرفر.
- أي هيدرز هوية يرسلها الزائر تُستبعد، فلا يمكن استخدامها لتجاوز الباسورد.
- حد المحاولات مشترك للحساب؛ مهاجم يعرف رابط الدخول يستطيع التسبب بقفل مؤقت. أضيفي Rate Limiting/WAF على مسارات password في Vercel للتخفيف من الإغراق قبل الوصول للدالة. لم يتم إعداد WAF من هنا.

## 5. استعادة الباسورد بالبريد — اختيارية

لإظهار زر نسيت كلمة المرور، أضيفي RESEND_API_KEY وRESEND_FROM من حساب Resend مع عنوان إرسال من نطاق معتمد، ثم أعيدي النشر. بدون الإعدادين الزر مخفي وAPI الاستعادة يرفض الطلب؛ لا يطبع الكود في logs ولا يدّعي أنه أرسل البريد.

أكواد الاستعادة عشوائية تشفيريًا، مخزنة ببصمة HMAC، وتنتهي بعد 15 دقيقة وتُستهلك مرة واحدة. تغيير الباسورد يلغي الجلسات والأكواد السابقة. إذا لم تفعلي البريد وفقدت الباسورد، يلزم إجراء استعادة بإدارة قاعدة البيانات؛ لا تغيري pepper ولا تحذفي بيانات الموقع بالكامل.

## 6. اختبارات وتشخيص

```sh
npm ci
npm run build
npm test
npm run test:vercel
```

تم تنفيذ 10 اختبارات CMS و5 اختبارات Vercel محليًا. تشمل إنشاء الجداول المتكرر، منع انتحال الهوية، حفظ المحتوى، رفض طلبات cross-origin، الدخول والخروج وحد المحاولات، مسارات rewrites، ومحاكاة تخزين الصور بـBlob. اختبار الصور استخدم مزود Blob وهميًا للتحقق من التكامل؛ لم يتم رفع صورة لحسابك فعليًا ولم يجر اختبار نشر حقيقي دون ربط حسابك وخدماتك.

- /api/content يجب أن يرجع JSON، وليس index.html أو 404.
- /admin يجب أن يعرض الدخول أو رسالة إعداد واضحة، لا 404.
- /api/admin/content بدون جلسة يجب أن يرجع 401 بعد اكتمال الإعداد.
- /admin يعرض 503: راجعي وجود متغيرات الإدارة وقاعدة البيانات وتوافق المحرك والجداول.
- الصور الجديدة لا ترفع: راجعي Public Blob token والحد 3 MB.
- 429 عند الدخول: انتظري 15 دقيقة.
- تعديلات local preview عبر npm run dev تظل مؤقتة كما في النسخة الأصلية؛ لا تشغلي scripts/dev.mjs كسيرفر إنتاج.

النسخة الأصلية تحتوي وظيفة حذف سلة المحذوفات بعد 30 يومًا داخل scheduled الخاص بـWorker. لم نربطها بـVercel Cron؛ يظل الحذف النهائي اليدوي من الأدمن متاحًا، ولا يوجد حذف تلقائي جديد خفي.

هذه التغييرات جاهزة للرفع، لكنها لم تُرفع إلى GitHub أو تُنشر على Vercel من هذه الجلسة.
