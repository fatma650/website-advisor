# البريد الموثق لمالك الموقع: يربط معرفه بالأدمن عند أول دخول فقط.
ADMIN_BOOTSTRAP_EMAIL=

# Secret random pepper, managed in Sites; keep stable after password setup.
ADMIN_PASSWORD_PEPPER=

# Vercel only: use a NEW dedicated remote libSQL database.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Initial secret, 12..128 characters. Remove after first successful login.
ADMIN_INITIAL_PASSWORD=
# Connect a PUBLIC Vercel Blob store for portfolio images.
BLOB_READ_WRITE_TOKEN=
# Optional password recovery, verified sender required.
RESEND_API_KEY=
RESEND_FROM=
